<?xml version="1.0" encoding="utf-8"?>
<!-- Layout para mensajes RECIBIDOS en un GRUPO -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingVertical="4dp"
    android:paddingHorizontal="8dp">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_alignParentStart="true"
        android:background="@drawable/chat_bubble_received"
        android:padding="10dp"
        android:layout_marginEnd="80dp">

        <!-- ¡NUEVO! Nombre del autor -->
        <TextView
            android:id="@+id/tvMessageAuthor"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="Nombre Autor"
            android:textColor="@color/naranja"
            android:textSize="12sp"
            android:textStyle="bold"
            android:layout_marginBottom="4dp"/>

        <TextView
            android:id="@+id/tvMessageText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@android:color/black"
            android:textSize="16sp"
            tools:text="Este es un mensaje recibido." />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_gravity="end"
            android:gravity="center_vertical"
            android:layout_marginTop="4dp">

            <TextView
                android:id="@+id/tvMessageTime"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="12:01"
                android:textColor="#80000000"
                android:textSize="10sp"
                android:layout_marginEnd="8dp"/>

            <!-- ¡NUEVO! Botón de Borrar (solo visible para el ADMIN) -->
            <ImageView
                android:id="@+id/ivDeleteMessage"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:src="@android:drawable/ic_menu_close_clear_cancel"
                app:tint="#AAAAAA"
                android:visibility="gone"
                tools:visibility="visible"
                android:contentDescription="Borrar mensaje" />

        </LinearLayout>
    </LinearLayout>

</RelativeLayout>